/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export", // new one 
  images: {
    domains: [
      "pexels.com",
      "res.cloudinary.com",
      "images.unsplash.com",
      "images.pexels.com",
    ],
  },
};

export default nextConfig;
